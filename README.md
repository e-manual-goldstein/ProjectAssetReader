This is a custom application for simplifying dependency analysis using the "project.assets.json" files which are produced when compiling ".csproj" and ".vbproj" SDK-style project files.

Work in Progress
